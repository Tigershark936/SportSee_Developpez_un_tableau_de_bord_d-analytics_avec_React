import NotFound from "../NotFound/NotFound"

/**
 * ⚠️ Page temporairement désactivée.
 * À activer une fois que le composant Settings est prêt.
 */

const Settings = () => {
       return (
        <NotFound />
    )
}

export default Settings