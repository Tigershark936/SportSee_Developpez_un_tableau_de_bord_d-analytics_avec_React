import NotFound from "../NotFound/NotFound"

/**
 * ⚠️ Page temporairement désactivée.
 * À activer une fois que le composant Community est prêt.
 */

const Community = () => {
    return (
        <NotFound />
    )
}

export default Community